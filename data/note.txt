1Dokcer Create -> backend database server
                 -> to presistence volume create
                 -> Change date to current by adding 7 years

docker volume create postgres-data  

docker run --name querymate-db \
  -e POSTGRES_PASSWORD=password \
  -e POSTGRES_USER=user \
  -e POSTGRES_DB=ecommerce \
  -p 5432:5432 \
  -v postgres-data:/var/lib/postgresql \
  -d postgres

docker exec -it querymate-db psql -U user -d ecommerce